version: "3"
services:
  webserver:
    build: "."
    container_name: goodvotes-web
    ports:
      - "$GOODVOTES_HOST:$GOODVOTES_PORT:80"
    volumes:
      - ./storage:/App/storage
    env_file:
      - .env